@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Socio - Forgot Password</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #ffffff;
        }
        .login-card {
            width: 950px;
            max-width: 95%;
            display: flex;
            background-color: #ffffff;
        }
        .login-image {
            flex: 1.5;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .login-image img {
            width: 100%;
            height: auto;
            object-fit: cover;
        }
        .login-form {
            flex: 1;
            padding: 50px 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        .login-form input {
            margin-bottom: 15px;
            background-color: #fafafa;
            border: 1px solid #dbdbdb;
            height: 35px;
            font-size: 16px;
            padding: 0 15px;
        }
        .login-form .btn {
            background-color: #0095f6;
            color: #fff;
            height: 40px;
            font-size: 16px;
            margin-top: 10px;
        }
        .signup-text {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
        }
        .signup-text a {
            text-decoration: none;
            color: #0095f6;
        }
        .instruction-text {
            font-size: 14px;
            text-align: center;
            color: #262626;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>

    <div class="login-card">
        <div class="login-image d-none d-md-flex">
            <img src="@Url.Content("~/images/landing-page-img.png")" alt="Forgot Password Image">
        </div>

        <div class="login-form">
            <div class="text-center mb-3">
                <img src="@Url.Content("~/images/landing-logo-img.png")" alt="Socio Logo" style="height:60px; width:auto;">
            </div>

            <p class="instruction-text">
                Enter your email and we’ll send you a link to reset your password.
            </p>

            <form method="post">
                <input type="email" name="email" class="form-control" placeholder="Email" required />
                <button type="submit" class="btn w-100">Send Reset Link</button>

                @if (ViewBag.Message != null)
                {
                    <div class="text-success text-center mt-3">@ViewBag.Message</div>
                }
            </form>

            <div class="signup-text">
                Remembered your password? <a asp-action="Login" asp-controller="UserAuth">Log In</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
